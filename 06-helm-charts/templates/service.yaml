apiVersion: v1 
kind: Service 
metadata:
  name: nginx-service 
  namespace: roboshop
spec:
  selector:
    app: nginx
    type: chart
    ports:
    - protocol: TCP 
      port: 80 
      targetPort: 80 